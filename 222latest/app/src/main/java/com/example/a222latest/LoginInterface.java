package com.example.a222latest;

public interface LoginInterface {

    // Default Login screen is called.

    boolean checkLogin(); // (OnClick Login button) Checks from Firebase if the username and password exists.

}
