<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group>
        <item
                android:id="@+id/posts"
                android:icon="@drawable/home_drawer_icon"
                android:title="Posts" />
        <item
                android:id="@+id/newPost"
                android:icon="@drawable/postadd_drawer_icon"
                android:title="New Post" />
        <item
                android:id="@+id/chat"
                android:icon="@drawable/chat_drawer_icon"
                android:title="Chats" />
        <item
                android:id="@+id/contact"
                android:icon="@drawable/contact_drawer_icon"
                android:title="Contact" />
        <item
                android:id="@+id/map"
                android:icon="@drawable/map_drawer_icon"
                android:title="Map" />
        <item
                android:id="@+id/seminar"
                android:icon="@drawable/seminar_drawer_icon"
                android:title="Seminar" />
        <item
                android:id="@+id/profile"
                android:icon="@drawable/profile_drawer_icon"
                android:title="Profile" />
        <item
                android:id="@+id/foodlist"
                android:icon="@drawable/food_drawer_icon"
                android:title="Food List" />
    </group>
</menu>